#include <iostream>
#include <vector>

using namespace std;

int n, m;
vector<int> eatable;

int main(){
  int case_no;
  cin >> case_no;

  vector<int> result;

  for(int it = 0; it < case_no; ++it){
    cin >> n >> m;

    vector<string> name;
    for(int i = 0; i < n; ++i){
      string input;
      cin >> input;
      name.push_back(input);
    }

    
  }
}
